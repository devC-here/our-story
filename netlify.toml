[build]
  publish = "."               # serve files from repo root

[functions]
  directory = "netlify/functions"

# Optional: redirect /api/* → /.netlify/functions/* for cleaner URLs
[[redirects]]
  from   = "/api/chapters"
  to     = "/.netlify/functions/chapters"
  status = 200
